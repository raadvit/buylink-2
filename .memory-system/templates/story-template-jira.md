h1. {name}
 - Epic: {epic}
 - Role: {role}
 - Status: {status}

h2. Why / Business Goal
 * {why}

h2. Design (Co se zobrazuje)
 - {figma_url}
 - {figma_thumbnail}
 - {what}

h2. Jak se to chová
 * {how}

h2. Rizikové situace
 * {scope}

h2. Otevřené otázky
 * {deps}

{arch}h2. Architecture Notes
 * 

h2. Domain Changes
 * 

h2. API Changes
 * 

h2. DB Changes
 * 

h2. Implementation Plan

h3. BE
 * krok 1

h3. FE
 * krok 1

h3. DB
 * migrace / schéma

h2. Dotčené soubory

*FE:*
 * `path/to/component.tsx` — popis změny

*BE:*
 * `path/to/module.py` — popis změny

*DB:*
 * `migrations/NNN_nazev.sql` — popis

h2. Test plán
 * BE unit testy: co testovat
 * FE unit testy: co testovat
 * Integrační testy: co testovat end-to-end

h2. Manuální QA scénář
 * krok 1 — akce
 * krok 2 — akce
 * Očekávaný výsledek: ...

h2. Metriky
 * 

h2. QA výsledek
 * 
